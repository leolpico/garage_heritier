{% extends "LPPlatformBundle::layout.html.twig" %}

{% block title %}
    Lecture d'une annonce - {{ parent() }}
{% endblock %}

{% block lpplatform_body %}

    <h2>Visionner une annonce</h2>

    <div style="float: left; margin-right: 1em;">
        {# On vérifie qu'une image soit bien associée à l'article #}
        {% if carAdvert.image is not null %}
            <img
                    src="{{ asset(carAdvert.image.webPath) }}"
                    alt="{{ carAdvert.image.alt }}"
                    height="60"
            />
        {% endif %}
    </div>

    <h2>{{ carAdvert.title}}</h2>
    <i> le {{ carAdvert.datePosted|date('d/m/Y') }}</i>

    <div class="well">
        <ul style="list-style-type:none">
            <li>
                Marque : {{ carAdvert.car.brand }}
            </li>
            <li>
                Modèle : {{ carAdvert.car.model }}
            </li>
            <li>
                Année : {{ carAdvert.car.year|date('Y') }}
            </li>
            <li>
                Description : {{ carAdvert.description }}
            </li>
            <li>
                prix : {{ carAdvert.car.price }}
            </li>
            <li>
                Kilométrage : {{ carAdvert.car.km }}
            </li>
            <li>
                Energie: {{ carAdvert.car.energy }}
            </li>
        </ul>

    </div>

    <p>
        <a href="{{ path('lp_platform_index') }}" class="btn btn-default">
            <i class="glyphicon glyphicon-chevron-left"></i>
            Retour à la liste
        </a>
    </p>

{% endblock %}